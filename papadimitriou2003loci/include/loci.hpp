#ifndef __LOCI_HPP__
#define __LOCI_HPP__

/*
 * This is the headfile of this program
 * Cause we want to process high dimension data, so we
 * use cv::Mat to store the data points not cv::Point
 * every row of the data matrix store a data point
 */

#include <opencv2/core/core.hpp>

class LOCI
{
    LOCI();
};

#endif